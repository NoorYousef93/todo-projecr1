'use strict';
 function question() {
        
    questionone = prompt("are your age between 18 and 34 yes or no " );
     questiontwo = prompt( " are your major in it field  yes or no " );
     questionthree = prompt( "are you LTUC student yes or no ");

 }
 if (questionone !== "yes" && questionone !== "no" )
 { var answer = "invalid" 
    else if ( questiontwo !== "yes" && questionone !== "no" );
     var answer = "invalid" ;
     else if ( questionthree !== "yes" && questionone !== "no" )
      var answer = "invalid" 
    else 
    for (var i=0 , i<3 , i++ ), 
    var theanswer=[ ]
    theanswer.push ()

 )
console.log(questionone)




